// SPDX-License-Identifier: Apache-2.0

/**
 * MAP-ATLAS demo — a generic field logger (no real domain).
 *
 * Phase 0 skeleton. The full record→pin→photo→review loop lands in Phase 7.
 */
import { VERSION as coreVersion } from "@mapatlas/core";

export function main(): string {
  return `MAP-ATLAS demo (core ${coreVersion})`;
}
