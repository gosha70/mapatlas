# The workspace declares node >=20.11 and the toolchain genuinely requires it — Vitest will
# not start on older runtimes. Without this, npm only warns, so a contributor on an old Node
# discovers the problem as a confusing failure at test time instead of a clear one at install.
engine-strict=true
